
(ns module-installer.state)

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

; @ignore
;
; @atom (map)
(def INSTALLERS (atom {}))

; @ignore
;
; @atom (map)
(def INSTALLATION-STATE (atom {}))
